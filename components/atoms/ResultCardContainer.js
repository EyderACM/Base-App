import { View } from "react-native";
import styled from "styled-components";
import * as colors from "../../styles/colors";

export const ResultCardContainer = styled.View`
  border-radius: 5;
  background-color: ${colors.mainColor};
  height: 120;
  width: 340;
`;
