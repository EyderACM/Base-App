import * as bases from "./baseValues";

export const sliderValues = [
  bases.base2.name,
  bases.base3.name,
  bases.base4.name,
  bases.base5.name,
  bases.base6.name,
  bases.base7.name,
  bases.base8.name,
  bases.base9.name,
  bases.base10.name,
  bases.base16.name,
];

export const calculatorValues = [1, 2, 3, 4, 5, 6, 7, 8, 9, "A", "B", "C", "D", "C", "D", "E", "F"];
